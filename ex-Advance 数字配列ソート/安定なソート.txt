安全ソート(stable sort)とは、並び替え(ソート)アルゴリズムのうち、
順位が同じ要素の並び順がソート前後で変わらない性質を持つもの。

例{2(1番目),1,2(2番目)}
の場合ソート後も最初に現れる2(1番目)が、
次に現れる2(2番目)の前にあることが保証される。

自分の書いたコード(バブルソート)は安定ソート
if(numbers[j] > numbers[j+1])
同じ値の場合、スワップ処理がされない為、